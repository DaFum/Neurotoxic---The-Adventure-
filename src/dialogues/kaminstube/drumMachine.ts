import { type Dialogue } from '../../store';
import { say } from '../shared/helpers';

export function buildKaminstubeDrumMachineDialogue(): Dialogue {
  return say('Ein alter TR-808, der aussieht, als wäre er in einem Hochofen geschmolzen. Er gibt nur noch ein rhythmisches Klacken von sich, das seltsam beruhigend wirkt. Lars: "Das ist das Herzstück der ersten NEUROTOXIC-Platte. Er ist gestorben, als wir versuchten, ihn an ein Atomkraftwerk anzuschließen."');
}