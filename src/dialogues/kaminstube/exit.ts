import { type Dialogue } from '../../store';
import { say } from '../shared/helpers';

export function buildKaminstubeExitDialogue(): Dialogue {
  return say('Auf zur SZaturday 3 Riff Night in Salzgitter!');
}